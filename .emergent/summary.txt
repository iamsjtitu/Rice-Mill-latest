<analysis>**original_problem_statement:**
The user is building a comprehensive management tool for a rice mill named Mill Entry System. The user's latest request is to fix the Windows desktop application, which has become non-functional after recent feature additions. The application is perpetually stuck on Loading... screens, new features (like Staff Management) do not save data, and all PDF/Excel export functionalities are broken. The user is frustrated with the recurring issues and the credits being spent, and has requested a thorough and permanent fix for the Windows software. After a series of fixes, the app was working, but the user now reports that after a short period of use, the desktop application again becomes unresponsive, showing error loading and Request failed with status code 404, and preventing login. This indicates a critical stability issue. The user also needs clear instructions on how to build the Windows  file.

**PRODUCT REQUIREMENTS:**
-   **P0: Fix Windows Desktop App Stability:** The application must be stable for long-term use. It should not crash or become unresponsive. All features, data loading, CRUD operations, and exports must remain functional.
-   **P1: Feature Parity:** All features from the primary Python backend must be fully and correctly implemented in the  and  Node.js backends. (This was mostly completed, but stability is now the blocker).
-   **P2: Auto-Open Downloads:** Downloaded PDF and Excel files should open automatically. (Implemented but needs verification by the user with the new build).
-   **P3: Automated Cash Book Entries:** Payments made or received for staff, trucks, agents, etc., should automatically reflect in the Cash Book. (Implemented, but needs user verification).

**User's preferred language**: Hindi

**what currently exists?**
The application has a React frontend, a stable Python/FastAPI backend, and two Node.js backends ( and ). The agent has performed extensive work to bring the Node.js backends to feature parity with the Python backend, fixing dozens of bugs related to API paths, missing endpoints, PDF generation (the '₹' symbol bug), and implementing cash book integration for all payment types. All test data has been cleaned from the database. However, the  suffers from a critical stability issue, where its internal Express server appears to crash silently after some period of use, causing all subsequent API calls to fail with 404 errors.

**Last working item**:
- Last item agent was working: Investigating the root cause of the  becoming unresponsive. The agent correctly deduced that the issue is not related to authentication or route ordering, but is likely due to an unhandled runtime exception causing the entire Express server process within Electron to crash. The agent was about to implement global error handling and crash protection in  to catch and log the specific error causing the failure.
- Status: IN PROGRESS
- Agent Testing Done: N
- Which testing method agent to use? Manual testing by curl. The agent needs to add comprehensive error logging and crash protection to . After modification, the next step is not to test for a specific feature, but to ensure the server process itself is more robust. The agent should then ask the user to build and run the application to see if the crash can be caught and logged.
- User Testing Done: N

**All Pending/In progress Issue list**:
-   **Issue 1: Windows Desktop App Becomes Unresponsive and Crashes (P0)**
    -   **Description:** The desktop application works for a short while, but then all network requests start failing with a 404 error. This makes the application unusable and requires a full restart. The root cause is believed to be an unhandled error crashing the Node.js Express server running inside the Electron  process.
    -   **Attempted fixes:** The agent ruled out login/token issues and route ordering. The investigation correctly pointed towards a silent server crash.
    -   **Next debug checklist:**
        1.  **Edit **.
        2.  Implement a global error handler for the Express app: . This will catch errors within the Express request-response cycle. Make sure to log the  to a file or the console.
        3.  Implement process-wide uncaught exception handlers at the top of :  and . These will catch errors outside of Express that could crash the entire Node.js process.
        4.  Inform the user about these changes and provide the build commands again, explaining that the goal is to capture the specific error message when the app fails.
        5.  Once the user provides the error log, analyze it and fix the underlying bug.
    -   **Why fix this issue and what will be achieved with the fix?** This is a critical, app-breaking bug that makes the desktop application unreliable and unusable. Fixing it will restore stability and complete the user's primary request.
    -   **Status:** IN PROGRESS
    -   **Is recurring issue?** Y
    -   **Should Test frontend/backend/both after fix?** backend
    -   **Blocked on other issue:** No

**In progress Task List**:
-   None. All focus is on the P0 stability issue.

**Upcoming and Future Tasks**
-   None. The user has not requested any new features. The sole priority is a stable and functional Windows application.

**Completed work in this session**
- **Full Feature Parity:** Ensured all 156 API endpoints from the Python backend are now implemented in both the  and  Node.js backends.
- **Comprehensive Cash Book Integration:** Implemented and verified that creating payments for Staff, Trucks, Agents, and MSP automatically generates corresponding Jama (credit) or Nikasi (debit) entries in the cash book across all three backends.
- **PDF Generation Fixes:**
    - Replaced all instances of the Rupee symbol '₹' with 'Rs.' in PDF generation code, eliminating the black box rendering error in .
    - Improved PDF table formatting to handle text wrapping and prevent column overflow.
- **Desktop App Enhancements:**
    - **File Auto-Open:** Modified the Electron  handler to automatically open PDF/Excel files after they are saved.
    - **Keyboard Shortcuts:** Added missing shortcuts for all navigation tabs and implemented the  key to display the shortcuts dialog.
- **Financial Year Logic:** Updated the FY dropdown to always include the upcoming year automatically.
- **Database Cleanup:** Purged all old test data from the MongoDB collections (, , , etc.), providing the user with a clean slate.
- **Bug Fixes & Stability:**
    - Fixed a bug that prevented admins from deleting certain database entries.
    - Resolved multiple Node.js server startup errors related to duplicate module declarations.
- **User Support:** Provided the user with correct, step-by-step command-line instructions to build the Windows installer, including how to handle PowerShell execution policy errors.

**Earlier issues found/mentioned but not fixed**
-   None.

**Known issue recurrence from previous fork**
-   **Issue recurrence in previous fork**: The  has been consistently unstable, though the root causes have varied (e.g., incorrect route ordering, missing endpoints, and now, runtime crashes).
-   **Recurrence count**: 3+
-   **Status**: IN PROGRESS

**Code Architecture**


**Key Technical Concepts**
-   **Multi-Backend Architecture:** The system uses three backends. The Python/FastAPI backend is the primary live server, while the two Node.js/Express backends are for the local and distributable desktop versions.
-   **Electron with Embedded Express:** The desktop app is an Electron application that runs a full Express.js server inside its main process () to serve the API and the React frontend.
-   **Runtime Stability:** The current critical issue is the lack of runtime stability in the 's Node.js process, which crashes on unhandled exceptions.
-   **PDF/Excel Generation:** Uses  and  in the Node.js backends.

**key DB schema**
- No changes to schema. The main collections are , , , , , .

**All files of reference**
-   **/app/desktop-app/main.js**: **CRITICAL FILE.** This monolithic file contains the Electron window setup, the Express server, and most of the route logic for the desktop app. It is the target for implementing crash protection and error logging.
-   **/app/local-server/routes/**: The modularized routes for the , which are more maintainable.
-   **/app/backend/routes/**: The Python route files, serving as the source of truth for business logic.

**Areas that need refactoring**:
-   The  file is over 3000 lines long and handles everything from window creation to all API logic. It is extremely fragile and hard to debug. While the user has previously rejected refactoring, it remains the largest technical debt and the likely source of instability. A long-term solution would involve breaking its routes into modular files, similar to the  architecture.

**key api endpoints**
-   All endpoints in  become unavailable (return 404) when the server crashes. There is no single failing endpoint; the entire process fails.

**Critical Info for New Agent**
-   **Language:** You **MUST** communicate with the user in **Hindi**.
-   **Top Priority (P0):** The desktop app is crashing. Your first and only task is to make it stable. Do not work on anything else.
-   **Your Strategy:** Do not guess the cause of the crash. Your immediate goal is to **instrument, not fix**. Add comprehensive , , and  error handlers to  to log the *exact* error that is causing the crash.
-   **User Collaboration:** Explain to the user that you've added logging to catch the error. Provide clear build instructions (, , ) and ask them to use the app until it fails again, then send you the error from the console/log file.
-   **Technical Debt:** Be aware that  is very fragile. Make targeted, minimal changes to add logging. Avoid any refactoring unless it's the only way to solve the crash.

**documents and test reports created in this job**
-   /app/memory/PRD.md

**Last 10 User Messages and any pending HUMAN messages**
1.  **User:** Asks for the command to build the Windows software.
2.  **User:** Shares a PowerShell error about script execution being disabled.
3.  **Agent:** Provides the command to change the PowerShell execution policy.
4.  **User:** Shares a new npm <command>

Usage:

npm install        install all the dependencies in your project
npm install <foo>  add the <foo> dependency to your project
npm test           run this project's tests
npm run <foo>      run the script named <foo>
npm <command> -h   quick help on <command>
npm -l             display usage info for all commands
npm help <term>    search for help on <term>
npm help npm       more involved overview

All commands:

    access, adduser, audit, bugs, cache, ci, completion,
    config, dedupe, deprecate, diff, dist-tag, docs, doctor,
    edit, exec, explain, explore, find-dupes, fund, get, help,
    help-search, hook, init, install, install-ci-test,
    install-test, link, ll, login, logout, ls, org, outdated,
    owner, pack, ping, pkg, prefix, profile, prune, publish,
    query, rebuild, repo, restart, root, run-script, sbom,
    search, set, shrinkwrap, star, stars, start, stop, team,
    test, token, uninstall, unpublish, unstar, update, version,
    view, whoami

Specify configs in the ini-formatted file:
    /root/.npmrc
or on the command line via: npm <command> --key=value

More configuration info: npm help config
Configuration fields: npm help 7 config

npm@10.8.2 /usr/lib/node_modules/npm error:  not found.
5.  **Agent:** Corrects the user, telling them to  into the  directory first.
6.  **User:** Reports that after working for a while, the software stopped working, showing error loading and Request failed with status code 404, and login failed after logging out. (THIS IS THE CURRENT P0 BUG).
7.  **Agent:** (Internal thought) Suspects a route ordering issue.
8.  **Agent:** (Internal thought) Rules out route ordering and token issues.
9.  **Agent:** (Internal thought) Correctly identifies the likely cause as the Express server crashing on an unhandled error.
10. **Agent:** (Internal thought) Plans to add crash protection and global error handlers to  to diagnose the problem.

**Project Health Check:**
-   **Broken:**  (Critically unstable, crashes during use).
-   **Mocked:** None.

**3rd Party Integrations**
-   **electron**, **electron-builder**: For building the Windows desktop application.
-   **exceljs**, **pdfkit**: For generating Excel and PDF reports in the Node.js backends.
-   **openpyxl**, **reportlab**: For generating reports in the Python backend.

**Testing status**
-   **Testing agent used after significant changes:** NO
-   **Troubleshoot agent used after agent stuck in loop:** NO
-   **Test files created:** None
-   **Known regressions:** The entire  is considered a regression due to its instability.

**Credentials to test flow:**
-   **Admin:**  / 
-   **Staff:**  / 

**What agent forgot to execute**
-   The agent correctly diagnosed the need for crash protection in the fragile  file but had not yet implemented it. The focus was on fixing specific functional bugs, but the underlying architectural fragility was not addressed with defensive programming (e.g., global error handlers), leading to the current stability crisis.</analysis>
